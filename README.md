# Luminar Insights API
A real-time data analytics and insights engine.
## Getting Started
1. Run `npm install` to install dependencies.
2. Run `npm start` to start the server.
## Usage
Use a tool like `curl` to test the API endpoints.